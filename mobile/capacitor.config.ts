import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.amigofiel.mobile',
  appName: 'Amigo Fiel',
  webDir: 'www',
  server: {
    cleartext: true
  }
};

export default config;
